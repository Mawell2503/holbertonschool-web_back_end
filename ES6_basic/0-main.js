// import links w exports
import { taskFirst, taskNext } from './0-constants.js';
// console brings your output to some form of dashboard 
// .log() gives action to the output which in tis case it to print
// backticks is used to print executable codes/function
// the mix of $ and {} branches the gap between the live code and the string(the space in between)
console.log(`${taskFirst()} ${taskNext()}`);
